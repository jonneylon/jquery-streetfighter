$(document).ready(function() {// GIF activates/deactivates when entering div area  $('.ryu').mouseenter(function() {    $('.ryu-still').hide();    $('.ryu-ready').show();  })  .mouseleave(function() {    $('.ryu-ready').hide();    $('.ryu-still').show();  }) // Ryu fires a Hadouken  .mousedown(function() { 	console.log('mousedown');  playHadouken();      $('.ryu-ready').hide();  $('.ryu-throwing').show();  $('.Hadouken').finish().show()  .animate(    {'left': '300px'},    500,    function() {      $(this).hide();      $(this).css('left', '-212px');    	});	}) // ryu goes back to his ready position  .mouseup(function() {    console.log('mouseup');    $('.ryu-throwing').hide();  	$('.ryu-ready').show();  })//Ryu goes into cool mode when "X" key is pressed  $("#keyboard").keydown(function(e){				if (e.keyCode == 88)				{			    	$('.ryu-still').hide();			    	$('.ryu-cool').show();			    };			}); 			$("#keyboard").keyup(function(e){				if (e.keyCode == 88)				{			    	$('.ryu-still').show();			    	$('.ryu-cool').hide();		    	}; });});// Sound is played when mouse is clickedfunction playHadouken () {  $('#hadouken-sound')[0].volume = .5;  $('#hadouken-sound')[0].load();  $('#hadouken-sound')[0].play();}